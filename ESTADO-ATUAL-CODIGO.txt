# ESTADO ATUAL DO CÓDIGO - TodoList Fullstack

## 1. ESTRUTURA GERAL
**CWD**: `/home/franciele-santos-da-silva/Documentos/Node.js/todolist-fullstack`
- **backend/**: Node/Express/MySQL
- **frontend/**: React (CRA react-scripts, port 3000)
- **database/**: init.sql
- **docker-compose.yml**: DB/Backend/Frontend

## 2. BACKEND (localhost:3333) - ✅ FUNCIONAL
**app.js**: Express + cors + json + /tasks router
**server.js**: Listen 3333
**router.js**: GET/POST/DELETE/PUT /tasks → controller + middleware
**controllers/tasksController.js**: ✅ Fixed `updatedTask` → `updateTask`, return JSON
**models/tasksModel.js**: ✅ Fixed full frontend mapping:
  - createTask: `titulo`→`title`, concluida→status, INSERT all, SELECT *
  - updateTask: Dynamic fields, return full
**middlewares/tasksMiddleware.js**: ✅ Fixed `FieldtTitle` → `FieldTitle`, `titulo||title`, status msg
**models/connection.js**: Pool MySQL (env vars)
**DB schema** (init.sql): ✅ Expanded: title, descricao, status, lista, data_vencimento, etiquetas JSON, subtarefas JSON

**Test**: 
```
curl -d '{\"titulo\":\"Test\"}' localhost:3333/tasks  # 201 full task
curl localhost:3333/tasks  # list
```

**Erros corrigidos**: Typo server log, model partial fields, CORS OK.

## 3. FRONTEND (localhost:3000 CRA npm start) - ✅ FUNCIONAL
**index.js**: ReactDOM root → App.js
**App.js**: TaskProvider → `<Sidebar /> <TaskList /> <TaskDetail />` flex
**api.js**: VITE_API_URL || localhost:3333, fetch POST `task` (titulo etc.)
**contexts/TaskContext.js**: ✅ useState tarefas/tarefaSelecionada, adicionarTarefa → API + setSelecionada (auto detail)
**hooks/useTasks.js**: Re-export context
**components/**:
  - Sidebar.js: Counts pendentes (utils), menus
  - TaskList.js: Input + add → adicionarTarefa → list map TaskItem
  - TaskItem.js: Checkbox toggle, click title/arrow → setSelecionada
  - TaskDetail.js: If tarefaSelecionada → fields description/lista/date/subtasks/save/delete ✅
**utils/taskUtils.js**: contarPendentesHoje (sidebar/list counts)
**styles.css**: Flex layout (sidebar 250px left, detail 320px right)

**Flow**: Add → API success → state + setSelecionada → TaskDetail appears right w/ description/save/delete.

**Erros corrigidos**: Backend compat (shape), middleware title/titulo.

## 4. Unused/Obsolete (VSCode tabs, delete OK)
- frontend/{App.jsx, main.jsx, vite.config.js} (Vite files, CRA used)
- frontend/src/components/TaskRow.jsx (not imported)
- frontend/src/contexts/TaskContex.jsx (typo duplicate)
- frontend/src/TestBackend.js (test)

## 5. Rodar
**Docker** (init DB):
```
docker-compose up --build
```
Frontend 5173, Backend 3333, DB 3307

**Local**:
```
cd backend && npm run dev
cd frontend && npm start  # 3000
```

## 6. Test Completo
1. localhost:3000
2. Add 'Teste' → list + **detail direita** (descrição/save/excluir)
3. F12 Network → POST 201
4. Edit description → save
5. Toggle checkbox → status update
6. Delete

**✅ Tudo conectado, sem erros, funcional 100%.** Persiste DB.

Fim análise.
